PROJECT 1 - Forking

I was tasked with using forking to implement a terminal interface. You had to
create a new fork to execute a command. The challenge here comes with implementing
pipes (|) and executing multiple commands (&).
